var numbers = [10, 12, 13, 14, 15, 16, 57, 58, 46];

//loop 
for (var i = 0; i < numbers.length; i++) {

    var element = numbers[i];
    console.log(element);
}

//another array

var items = ['bottle', 'glass', 'mirror', 'makeup', 'phone'];

for (i = 0; i < items.length; i++) {
    var item = items[i];
    //items[i] give the value of i index
    console.log(item);
}